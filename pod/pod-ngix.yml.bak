apiVersion: v1
kind: Pod
metadata:
  name: demo-nginx
  labels:
    app: nginx    # ← THIS IS REQUIRED
spec:
  containers:
  - name: nginx
    image: nginx:stable
    ports:
    - containerPort: 80
